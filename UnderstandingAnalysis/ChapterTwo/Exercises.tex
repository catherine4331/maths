\documentclass{article}
\usepackage{amsmath}
\usepackage{amsthm}

\newtheorem{theorem}{Theorem}

\author{Catherine Reid}
\title{Chapter Two Theorems \& Exercises}

\begin{document}

\maketitle

\textbf{Theorem 2.5.5 (Bolzano-Weierstrass)} Every bounded sequence contains a convergent subsequence
\begin{proof}
    Let $(a_n)$ be a bounded sequence so that there exists $M > 0$ such that $|a_n| \le M$ for all $n \in N$. Bisect
    the closed interval $[-M, M]$ into the two closed intervals $[-M, 0]$ and $[0, M]$. It must be the case that at least one of
    these intervals contains an infinte number of terms of $(a_n)$. Choose an interval for which this is the case and label it $I_1$.
    Let $a_{n_1}$ be some term from $(a_n)$ such that $a_{n_1} \in I_1$.

    Now, bisect $I_1$ into closed intervals of equal length and let $I_2$ be one containing an infinte number of terms
    of $(a_n)$. Because we have an infinite number of terms to choose from, it must be possible to select $n_2$ such that
    $a_{n_2} \in I_2$ and $n_2 > n_1$.

    In general, construct $I_{k}$ by bisecting $I_{k-1}$ into two closed intervals of equal length and selecting one which
    has an infinite number of terms of $(a_n)$ in it. We then select $n_k$ such that $n_k > n_{k-1} > ... > n_1$ and $a_{n_k} \in I_k$

    We now claim that the subsequence $(a_{n_k})$ is a convergent subsequence. Firstly we need a candidate for the limit. Since we have that
    \begin{align*}
        I_1 \subseteq  I_2 \subseteq I_3 \subseteq  I_4 \subseteq \cdots
    \end{align*}
    we know that by the nested interval principle there exists some element $a \in \mathbf{R}$ such that $a \in {\bigcap}_{k=1}^\infty I_k$.
    We will now show that $(a_{n_k}) \to a$
    Let $\varepsilon > 0$. By construction the length of $I_k$ is $M(1/2)^{k-1}$ which converges to 0. Choose $N$ so that
    $k \ge N$ implies that the length of $I_k$ is less that $\varepsilon$. Since both $a$ and $a_{n_k}$ are in $I_k$, this implies
    that $|a_{n_k} - a| < \varepsilon$.
\end{proof}
\pagebreak

\textbf{Exercise 2.5.4.} Assume that the nested interval principle is true and use it to provide a proof of the axiom of completeness.
Assume that $(1/2^n) \to 0$.

\begin{proof}
    Let $A \subset \mathbf{R}$ be non-empty and bounded above. We begin by considering the closed interval $[a, b]$ where $a \in A$ and
    $b$ is an upper bound of $A$. Label this interval $I_1$. In general we will construct the interval $I_k$ by bisecting $I_{k-1}$ into
    two closed intervals of equal length. We will call the left subinterval $R_{k-1}$ and the right subinterval $L_{k-1}$. We then use the
    following rule to construct $I_k$:
    \begin{align*}
        I_k = 
        \begin{cases} 
        R_{k-1}, & \text{if } A \cap R_{k-1} \ne \emptyset \\
        L_{k-1}, & \text{otherwise}
    \end{cases}
    \end{align*}

    The sets
    \begin{align*}
        I_1 \subseteq  I_2 \subseteq I_3 \subseteq  I_4 \subseteq \cdots
    \end{align*}
    form a nested sequence of closed intervals, and by the nested interval principle there exists at least one point $s \in \mathbf{R}$ contained
    in every $I_k$. We claim that $s = \sup(A)$.
\end{proof}

\textbf{Exercise 2.5.5.} Assume $(a_n)$ is a bounded sequence with the property that every convergent subsequence of $(a_n)$ converges
to the same limit $a \in \textbf{R}$. Show that $(a_n)$ must converge to $a$.

\begin{proof}
    Assume for the sake of contradiction that $(a_n)$ does not converge to $a$. By the defintion of convergence, this means that
    there exists an $\varepsilon_0 > 0$ and a subsequence $(a_{n_k})$ of $(a_n)$ such that $|a_{n_k} - a| \ge \varepsilon_0$ for all $k$.

    Since $(a_n)$ is bounded, it follows that $(a_{n_k})$ is also bounded. Therefore by the Bolzano-Weierstrass theorem there exists a convergent
    subsequence $(a_{n_{k_l}})$. Since this is also a subsequence of $(a_n)$, by our assumption we know that it must converge to $a$.

    By definition, this means that there exists some $N \in \textbf{N}$ such that for all $l \ge N$, $|a_{n_{k_l}} - a| < \varepsilon_0$. However,
    this contradicts our earlier claim that for every term in $(a_{n_k})$, $|a_{n_k} - a| \ge \varepsilon_0$.
    
    Therefore, by contradiction, we have shown that $(a_n)$ converges to $a$.
\end{proof}

\textbf{Exercise 2.5.6.} Show that $\lim(b^{1/n})$ exists for all $b \ge 0$ and find the value of the limit

\begin{proof}
    We will proceed by cases:

    \textbf{Case 1:} $b = 0$. In this case $b^{1/n}$ is equal to $0$ for all $n$, so the limit is equal to zero

    \textbf{Case 2:} $b = 1$. In this case $b^{1/n}$ is equal to $1$ for all $n$, so the limit is equal to one
    
    \textbf{Case 3:} $b > 1$. In this case we have
    \begin{align*}
        b > b^{1/2} > b^{1/3} > b^{1/4} > ... > 1
    \end{align*}
    so the sequence $(b^{1/n})$ is decreasing and bounded below. The Monotone Convergence Theorem allows us to conclude that
    $(b^{1/n})$ converges to some limit $l$ satsifying $b > l \ge 1$. To compute $l$, note that $(b^{1/2n})$ is a subsequence, so
    $(b^{1/2n}) \to l$. $b^{1/2n} = \sqrt{b^{1/n}}$, so by Exercise 2.3.1 $(b^{1/2n}) \to \sqrt{l}$. Limits are unique, so $\sqrt{l} = l$.
    Therefore we conclude that $l = 1$.

    \textbf{Case 4:} $0 < b < 1$. In this case we have
    \begin{align*}
        b < b^{1/2} < b^{1/3} < b^{1/4} < ... < 1
    \end{align*}
    so the sequence $(b^{1/n})$ is increasing and bounded above. The Monotone Convergence Theorem allows us to conclude that
    $(b^{1/n})$ converges to some limit $l$ satsifying $b < l \le 1$. To compute $l$, note that $(b^{1/2n})$ is a subsequence, so
    $(b^{1/2n}) \to l$. $b^{1/2n} = \sqrt{b^{1/n}}$, so by Exercise 2.3.1 $(b^{1/2n}) \to \sqrt{l}$. Limits are unique, so $\sqrt{l} = l$.
    Therefore we conclude that $l = 1$.
\end{proof}

\textbf{Exercise 2.5.7.} Extend the result proved in example 2.5.3 to the case $|b| < 1$.

\end{document}